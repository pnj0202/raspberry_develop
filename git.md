# git 사용법

## git 기본명령어
```
저장소 내려받음 ::::::::::::git clone
저장소 생성 ::::::::::::::::git init
저장소 파일 추가설정 :::::::git add 파일이름 (ex git add *)
저장소에 커밋 등록 :::::::::git commit -m " 커밋내용"
저장소에 파일등록 ::::::::::git push

저장소에 수정내역 제출 ::::: git commit , git comit -a (변경된 모든 파일), git commit -m " 
```
